//! Shared RPC contracts for the docs site.
//!
//! Wasm (`frontend/app.yuga`) and macOS (`macos/app.yuga`) POST `Docs.Page`.
//! `#[proto]` injects `encode_*` / `decode_*`.

#[proto]
struct PageReq {
    id: int,
}

#[proto]
struct PageResp {
    title: string,
    lead: string,
    body: string,
    sample: string,
}

fn page_rpc() -> string {
    return "Docs.Page"
}

fn page_count() -> int {
    return 16
}

fn native_addr() -> string {
    return "127.0.0.1:8082"
}
