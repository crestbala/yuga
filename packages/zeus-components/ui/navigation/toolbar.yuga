import "std:zeus"
import "../../theme.yuga"

// Toolbar: a row of actions. Children come from the caller's block.

fn Toolbar() -> Node {
    let n = Box(align_direction = DIRECTION.Row, spacing = SPACE.Md, align_items = ALIGN.Center)
    return n
}
