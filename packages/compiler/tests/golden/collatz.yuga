import "std:fmt"

fn collatz(n: int) {
    fmt.println(n)
    if n == 1 {
        return
    }
    if n % 2 == 0 {
        collatz(n / 2)
    } else {
        collatz(3 * n + 1)
    }
}

fn main() {
    collatz(6)
}
