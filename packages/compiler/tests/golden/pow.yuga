import "std:fmt"

fn pow(base: int, exp: int) -> int {
    let mut r = 1
    for i in 0..exp {
        r *= base
    }
    return r
}

fn main() {
    fmt.println(pow(2, 0))
    fmt.println(pow(2, 10))
    fmt.println(pow(3, 5))
}
