import "std:net"
import "std:fmt"
import "std:sys"

fn must(ok: bool) {
    if !ok {
        sys.exit(1)
    }
}

fn main() {
    let fd = net.tcp_listen(0)
    must(fd >= 0)
    let p = net.tcp_bound_port(fd)
    must(p > 0)
    must(p < 65536)
    net.tcp_close(fd)
    fmt.println(p)
}
