import "std:fmt"
import "std:sys"

fn add1(n: int) -> int {
    n + 1
}

fn fail() {
    sys.exit(1)
}

fn main() {
    let v = add1(1)
    if v < 2 {
        fail()
    }
    if 2 < v {
        fail()
    }
    fmt.println("tail")
}
