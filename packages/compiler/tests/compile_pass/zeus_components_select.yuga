import "std:fmt"
import "std:sys"
import "std:zeus"

let mut opts: []string = []string {}

fn Menu(sel: Signal<int>) {
    zeus.Select(sel, opts)
}

fn main() {
    push(opts, "A")
    push(opts, "B")
    let sel = zeus.signal(1)
    zeus.snapshot(240, 80, fn() -> Node {
        zeus.Row(SPACE.Sm) {
            Menu(sel)
        }
    })
    if zeus.draw_count() < 1 {
        fmt.println("FAIL select")
        sys.exit(1)
    }
    fmt.println("select ok")
}
