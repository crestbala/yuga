import fmt

fn gcd(a: int, b: int) -> int {
    if b == 0 {
        return a
    }
    return gcd(b, a % b)
}

fn main() {
    fmt.println(gcd(48, 18))
    fmt.println(gcd(17, 13))
    fmt.println(gcd(100, 25))
}
