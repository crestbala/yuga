import fmt

struct Rect {
    w: int,
    h: int,
}

fn area(r: &Rect) -> int {
    return r.w * r.h
}

fn grow(r: &mut Rect, n: int) {
    r.w += n
    r.h += n
}

fn main() {
    let mut r = Rect { w: 3, h: 4 }
    fmt.println(area(r))
    grow(r, 2)
    fmt.println(area(r))
}
