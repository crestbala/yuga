import fmt

fn sign(n: int) -> int {
    if n < 0 {
        return -1
    } else if n == 0 {
        return 0
    } else {
        return 1
    }
}

fn main() {
    fmt.println(sign(-7))
    fmt.println(sign(0))
    fmt.println(sign(42))
}
