import fmt

fn ack(m: int, n: int) -> int {
    if m == 0 {
        return n + 1
    }
    if n == 0 {
        return ack(m - 1, 1)
    }
    return ack(m - 1, ack(m, n - 1))
}

fn main() {
    fmt.println(ack(2, 2))
    fmt.println(ack(3, 2))
}
