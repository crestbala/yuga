import "std:zeus"

fn main() {
    App("t", 400, 300, fn() {
        Box(align_direction = "column", spacing = 0) {
            Box(align_direction = "row", spacing = 8, align_items = align_center()) {
                Text("a"),
                Spacer()
            },
            Divider()
        }
    })
}
