import "std:fmt"

fn main() {
    let a = [4]int { 10, 20, 30, 40 }

    fmt.println("len 4, valid slots are 0..3")
    fmt.println(a[0])
    fmt.println(a[3])

    // Out of bounds — Yuga traps instead of reading past the array.
    let i = 4
    fmt.println(a[i])
}
