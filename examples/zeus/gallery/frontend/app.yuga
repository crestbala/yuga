//! Canvas2D gallery UI. Same Zeus tree as macOS; Vite serves this wasm.

import "std:zeus"
import "../screen.yuga"

fn main() {
    let win = zeus.window_size()
    zeus.App("Zeus Kit", win.width, win.height, fn() {
        screen.Screen()
    })
}
